import { mockGoals } from '../mock/mockGoals'
import { mockRules } from '../mock/mockRules'

export const investmentGoal = mockGoals

export const investmentRules = {
  globalIndexBase: mockRules[0],
  kfInternationalCompounders: mockRules[1],
  highRiskLimit: mockRules[2],
  dividendGoal: mockRules[3],
}
